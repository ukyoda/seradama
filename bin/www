#!/usr/bin/env node
var debug = require('debug')('sera-dev2');
var app = require('../app');


app.set('port', process.env.PORT || 8080);

var server = app.listen(app.get('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});

//////////////////////////
// ここからした。
//////////////////////////

var engine = require('./model/functions/gameEngine');
var net = require('./model/functions/gameInterface');
var tools = require('./model/functions/tools');

var getNextStage = require('./model/functions/getNextStage');
var makeStage = require('./model/functions/makeStage');


var Goal = require('./model/objects/Goal');
var Kabe = require('./model/objects/Kabe');
var Maru = require('./model/objects/Maru');
var Movable = require('./model/objects/Movable');
var MovableMaru = require('./model/objects/MovableMaru');
var Player = require('./model/objects/Player');

var startTime = 0;
var maxTime = 180000;



// 平行世界
var funnyWorld = engine.createWorld(0.0, -20.0, true);
var funnyWorldIsAlive = false;

// Socket.IOのオブジェクト
var socketIO = require('socket.io');
var io = socketIO.listen(server);

//初回のステージ取得
stageData = makeStage(getNextStage(), world, players);

function mainLoop() {}

